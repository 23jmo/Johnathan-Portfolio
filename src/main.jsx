import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(<App />);
// this connects with a single div in the html and renders out all our react stuff
// ctrl + space to auto import
// install tailwind with vite
// tailwind.config.js configures additional colors

// react router dom is react's routing component
